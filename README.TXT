# To start all the tests you need to install all the packages from the file requirements.txt;
# From the root directory execute command:
# cd Tests && pytest -vvvv
#
#
#
# !!! If tests aren't working update/intall latest Chrome version
# !!! and put matching version of chromedriver.exe (download here - https://chromedriver.chromium.org/downloads)
# !!! into folder Components.

# Чтобы запустить все тесты нужно установить все пакеты из файла requirements.txt;
# Из корневой папки проект выполнить команду:
# cd Tests && pytest -vvvv
#
#
#
# !!! Если тесты не запускаются, обновите/установите последнюю версию Chrome
# !!! и закиньте соответствующую версию chromedriver.exe (скачать отсюда - https://chromedriver.chromium.org/downloads)
# !!! в папку Components.
